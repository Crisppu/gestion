/** @type {import('next').NextConfig} */
const nextConfig = {
    //esta regla me permite que las imagenes que venga de esta url esten listas antes de mostrarlas
    images: {
        domains: ['lh3.googleusercontent.com'],
    },
};


export default nextConfig;

